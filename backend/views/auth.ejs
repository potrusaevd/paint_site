<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Вход в аккаунт | Lime Details</title>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/auth.css">
</head>
<body>
    <header class="header">
        <div class="container">
            <div class="logo">
                <a href="/" onclick="window.location.href='Lime Ditails(osn).html'; return true;">Lime Details</a>
            </div>
            <a href="/" class="back-link" onclick="window.location.href='Lime Ditails(osn).html'; return true;">← На главную</a>
        </div>
    </header>

    <main class="auth-container">
        <div class="auth-wrapper">
            <div class="auth-form-panel">
                <h1>Добро пожаловать!</h1>
                <p class="auth-subtitle">Войдите в свой аккаунт или создайте новый</p>

                <div class="auth-tabs">
                    <button class="auth-tab active" data-tab="login">Вход</button>
                    <button class="auth-tab" data-tab="register">Регистрация</button>
                </div>

                <div id="messageContainer"></div>
                <div class="loading" id="loadingIndicator">Обрабатываем запрос...</div>

                <form class="auth-form active" id="loginForm">
                    <div class="form-group">
                        <label for="loginEmail">Email</label>
                        <input type="email" id="loginEmail" name="email" required
                               placeholder="example@mail.com">
                    </div>
                    <div class="form-group">
                        <label for="loginPassword">Пароль</label>
                        <input type="password" id="loginPassword" name="password" required
                               placeholder="Введите ваш пароль">
                    </div>
                    <div class="form-actions">
                        <button type="submit" class="btn-primary">Войти в аккаунт</button>
                        <a href="javascript:void(0);" class="forgot-password" onclick="handleForgotPassword();">Забыли пароль?</a>
                    </div>
                </form>

                <form class="auth-form" id="registerForm">
                    <div class="form-group">
                        <label for="registerUsername">Имя пользователя</label>
                        <input type="text" id="registerUsername" name="username" required
                               placeholder="Как к вам обращаться?">
                    </div>
                    <div class="form-group">
                        <label for="registerEmail">Email</label>
                        <input type="email" id="registerEmail" name="email" required
                               placeholder="example@mail.com">
                    </div>
                    <div class="form-group">
                        <label for="registerPassword">Пароль</label>
                        <input type="password" id="registerPassword" name="password" required
                               placeholder="Минимум 8 символов">
                    </div>
                    <div class="form-group">
                        <label for="confirmPassword">Подтверждение пароля</label>
                        <input type="password" id="confirmPassword" name="passwordConfirm" required
                               placeholder="Повторите пароль">
                    </div>
                    <div class="form-actions">
                        <button type="submit" class="btn-primary">Создать аккаунт</button>
                    </div>
                </form>
            </div>

            <div class="auth-info-panel">
                <div class="auth-info-content">
                    <h2>Lime Details</h2>
                    <p>Ваш надежный поставщик автозапчастей с широким ассортиментом и быстрой доставкой.</p>

                    <ul class="features-list">
                        <li>Огромный каталог оригинальных запчастей</li>
                        <li>Поиск по VIN и марке автомобиля</li>
                        <li>Быстрая доставка по всей стране</li>
                        <li>Гарантия качества на все товары</li>
                        <li>Профессиональная консультация</li>
                        <li>Специальные цены для постоянных клиентов</li>
                    </ul>
                </div>
            </div>
        </div>
    </main>

    <%- include('partials/_modals') %>

    <footer class="footer">
        <p>&copy; 2025 Lime Details. Все права защищены.</p>
    </footer>

    <script src="/js/auth.js" defer></script>
    <script src="/js/global-utils.js" defer></script>
    <script src="/js/clickSpark.js" defer></script>
<script>
    setTimeout(() => {
        try {
            if (typeof ClickSpark !== 'undefined') {
                new ClickSpark(document.body, {
                    sparkColor: "#4fa690",
                    sparkSize: 10,
                    sparkRadius: 20,
                    sparkCount: 8,
                    duration: 400,
                    easing: "ease-out",
                    extraScale: 1.0,
                });
                console.log('ClickSpark initialized with delay.');
            }
        } catch (error) {
            console.error('Error initializing ClickSpark with delay:', error);
        }
    }, 2000);
</script>
</body>
</html>